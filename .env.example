# Configuración de KashFlow

BOT_TOKEN=tu_bot_token_aqui
WEBHOOK_URL=https://tu-dominio.com
WEBHOOK_PORT=8443
API_PORT=3000
NODE_ENV=development

# Configuración de Whisper (Transcripción de Audio)
# Usar Whisper local (true) o API de OpenAI (false)
USE_LOCAL_WHISPER=true

# Modelo de Whisper local (solo si USE_LOCAL_WHISPER=true)
# Opciones: tiny, base, small, medium, large
# tiny: Más rápido, menos preciso (~1GB RAM)
# base: Balance velocidad/precisión (~1GB RAM) - RECOMENDADO
# small: Buena precisión (~2GB RAM)
# medium: Alta precisión (~5GB RAM)
# large: Máxima precisión (~10GB RAM)
WHISPER_MODEL=base

# API Key de OpenAI (solo si USE_LOCAL_WHISPER=false)
# Obtén tu key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=tu_openai_api_key_aqui
